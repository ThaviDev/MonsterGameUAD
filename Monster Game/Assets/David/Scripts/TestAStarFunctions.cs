using Pathfinding;
using UnityEngine;

public class TestAStarFunctions : MonoBehaviour
{
    [SerializeField] AstarPath _myGraph;
    [SerializeField] FollowerEntity _follower;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //_myGraph.IsPointOnNavmesh(_follower.transform.position);
        //_follower.
    }
}
